<UserControl x:Class="Lab04_Valeriano.Views.CategoriaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Lab04_Valeriano.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <vm:CategoriaViewModel/>
    </UserControl.DataContext>
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <DataGrid Grid.Column="0" ItemsSource="{Binding Categorias}"
                  SelectedItem="{Binding CategoriaSeleccionada}"
                  AutoGenerateColumns="False" IsReadOnly="True" Margin="0,0,10,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding CategoriaID}" Width="60"/>
                <DataGridTextColumn Header="Nombre" Binding="{Binding NombreCategoria}" Width="150"/>
                <DataGridTextColumn Header="Descripcion" Binding="{Binding Descripcion}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Column="1">
            <TextBlock Text="Nombre"/>
            <TextBox Text="{Binding CategoriaSeleccionada.NombreCategoria, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

            <TextBlock Text="Descripcion"/>
            <TextBox Text="{Binding CategoriaSeleccionada.Descripcion, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

            <UniformGrid Columns="2" Rows="2">
                <Button Content="Cargar" Command="{Binding CargarCommand}" Margin="2"/>
                <Button Content="Nuevo" Command="{Binding NuevoCommand}" Margin="2"/>
                <Button Content="Guardar" Command="{Binding GuardarCommand}" Margin="2"/>
                <Button Content="Eliminar" Command="{Binding EliminarCommand}" Margin="2"/>
            </UniformGrid>
        </StackPanel>
    </Grid>
</UserControl>
