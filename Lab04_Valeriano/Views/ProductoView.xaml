<UserControl x:Class="Lab04_Valeriano.Views.ProductoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Lab04_Valeriano.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="900">
    <UserControl.DataContext>
        <vm:ProductoViewModel/>
    </UserControl.DataContext>
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <DataGrid Grid.Column="0" ItemsSource="{Binding Productos}"
                  SelectedItem="{Binding ProductoSeleccionado}"
                  AutoGenerateColumns="False" IsReadOnly="True" Margin="0,0,10,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ProductoID}" Width="50"/>
                <DataGridTextColumn Header="Nombre" Binding="{Binding NombreProducto}" Width="150"/>
                <DataGridTextColumn Header="CategoriaID" Binding="{Binding CategoriaID}" Width="80"/>
                <DataGridTextColumn Header="ProveedorID" Binding="{Binding ProveedorID}" Width="80"/>
                <DataGridTextColumn Header="Precio" Binding="{Binding PrecioUnidad}" Width="80"/>
                <DataGridTextColumn Header="Existencia" Binding="{Binding UnidadesEnExistencia}" Width="80"/>
                <DataGridTextColumn Header="Descontinuado" Binding="{Binding Descontinuado}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <ScrollViewer Grid.Column="1">
            <StackPanel>
                <TextBlock Text="Nombre"/>
                <TextBox Text="{Binding ProductoSeleccionado.NombreProducto, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>

                <TextBlock Text="CategoriaID"/>
                <TextBox Text="{Binding ProductoSeleccionado.CategoriaID, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>

                <TextBlock Text="ProveedorID"/>
                <TextBox Text="{Binding ProductoSeleccionado.ProveedorID, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>

                <TextBlock Text="Cantidad por Unidad"/>
                <TextBox Text="{Binding ProductoSeleccionado.CantidadPorUnidad, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>

                <TextBlock Text="Precio Unidad"/>
                <TextBox Text="{Binding ProductoSeleccionado.PrecioUnidad, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>

                <TextBlock Text="Unidades en Existencia"/>
                <TextBox Text="{Binding ProductoSeleccionado.UnidadesEnExistencia, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>

                <TextBlock Text="Unidades en Pedido"/>
                <TextBox Text="{Binding ProductoSeleccionado.UnidadesEnPedido, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>

                <TextBlock Text="Nivel de Reorden"/>
                <TextBox Text="{Binding ProductoSeleccionado.NivelDeReorden, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>

                <CheckBox Content="Descontinuado" IsChecked="{Binding ProductoSeleccionado.Descontinuado}" Margin="0,0,0,8"/>

                <UniformGrid Columns="2" Rows="2">
                    <Button Content="Cargar" Command="{Binding CargarCommand}" Margin="2"/>
                    <Button Content="Nuevo" Command="{Binding NuevoCommand}" Margin="2"/>
                    <Button Content="Guardar" Command="{Binding GuardarCommand}" Margin="2"/>
                    <Button Content="Eliminar" Command="{Binding EliminarCommand}" Margin="2"/>
                </UniformGrid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
